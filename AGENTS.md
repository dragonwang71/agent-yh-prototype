# Repository guidance

このリポジトリでは、利用者向けの分かりやすさ、外部データへの忠実性、引き継ぎやすさを優先する。

## 実装

- UI、agent orchestration、外部 API adapter、決定的な fallback を分離する。
- モデル出力や外部 API 応答を無条件に信頼せず、UI 境界で契約を検証する。
- 取得できなかった価格、評価、場所、天気を推測して補わない。
- 日本語を既定言語とし、英語・中国語の既存動作も維持する。
- 失敗時は利用者向けの短い説明を返し、技術詳細は実行ログに置く。
- 新しい依存関係は目的と保守負担を説明できる場合だけ追加する。

## 変更手順

1. 関連する既存実装と harness case を確認する。
2. 変更を小さな責務単位で実装する。
3. 期待動作を harness またはテストへ追加する。
4. `npm run check` を実行する。
5. UI 変更はデスクトップとモバイルの主要フローを確認する。

## レビューと引き継ぎ

- PR には変更理由、利用者への影響、確認方法、残るリスクを書く。
- prompt やモデルを変更した場合は、対象ケースと評価結果を記録する。
- 秘密情報、個人情報、実際の会話履歴を fixture やログへ追加しない。
